# AGENTS.md

## Deploy
Push to `main` → auto-deploys to GitHub Pages at https://rodionsibov.github.io

## Branch
- `main`: live/production

## Local development
Run `bun index.html` to serve locally, or open `index.html` directly in browser. No build step required.

## Stack
- HTML + vanilla JS
- TailwindCSS via CDN
- No tests, no linting